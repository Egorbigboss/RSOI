language: python # => 1
    python: # => 2
      - "3.7"
    #services: # => 3
      #- sqlite
    env: # => 4
      -DJANGO=2.2 DB=sqlite
    install: # => 5
      - pip install Django
      - pip install djangorestframework
    script: # => 7
      - python manage.py runserver
